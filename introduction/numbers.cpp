#include <iostream>
#include <cmath>

using namespace std;

int main()
{
   int wnum = 5;
   double pnum = 5.3;

   //wnum++;
   wnum += 56;// wnum = wnum + 56
   cout << wnum << endl;
   cout << 10 % 3 << endl;//modulus is to get the remainder
   cout << pow(2, 5) << endl;
   cout << sqrt(23)<< endl;
   cout << round(2.1)<< endl;
   cout << ceil(2.1) << endl;
   cout << floor(2.6) << endl;
   cout << fmax(2, 10) << endl;
   cout << fmin(2, 10) << endl;
   


   return 0;
    
}